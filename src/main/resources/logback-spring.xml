<configuration>
  <property name="LOG_PATH" value="${LOG_PATH:-/app/logs}" />
  <timestamp key="byDay" datePattern="yyyy-MM-dd" />

  <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n</pattern>
    </encoder>
  </appender>

  <!-- Server logs go to dated file: app-YYYY-MM-DD.log -->
  <appender name="SERVER" class="ch.qos.logback.core.rolling.RollingFileAppender">
    <file>${LOG_PATH}/app-${byDay}.log</file>
    <rollingPolicy class="ch.qos.logback.core.rolling.FixedWindowRollingPolicy">
      <fileNamePattern>${LOG_PATH}/app-${byDay}.%i.log</fileNamePattern>
      <minIndex>1</minIndex>
      <maxIndex>5</maxIndex>
    </rollingPolicy>
    <triggeringPolicy class="ch.qos.logback.core.rolling.SizeBasedTriggeringPolicy">
      <maxFileSize>200MB</maxFileSize>
    </triggeringPolicy>
    <encoder>
      <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n</pattern>
    </encoder>
  </appender>

  <!-- Other logs go to app.log (active single file) -->
  <appender name="OTHER" class="ch.qos.logback.core.rolling.RollingFileAppender">
    <file>${LOG_PATH}/app.log</file>
    <rollingPolicy class="ch.qos.logback.core.rolling.FixedWindowRollingPolicy">
      <fileNamePattern>${LOG_PATH}/app.log.%i</fileNamePattern>
      <minIndex>1</minIndex>
      <maxIndex>5</maxIndex>
    </rollingPolicy>
    <triggeringPolicy class="ch.qos.logback.core.rolling.SizeBasedTriggeringPolicy">
      <maxFileSize>100MB</maxFileSize>
    </triggeringPolicy>
    <encoder>
      <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n</pattern>
    </encoder>
  </appender>

  <!-- Route exception package logs to SERVER (dated) -->
  <logger name="com.turf.battlegrounds.exception" level="WARN" additivity="false">
    <appender-ref ref="SERVER"/>
  </logger>

  <!-- Root sends everything else to OTHER (app.log) -->
  <root level="INFO">
    <appender-ref ref="CONSOLE" />
    <appender-ref ref="OTHER" />
  </root>

</configuration>
