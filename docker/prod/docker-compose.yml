services:
  app:
    image: spring-boot-prod:latest
    build:
      # Build context is the project root
      context: ..
      dockerfile: docker/prod/Dockerfile
    container_name: spring-boot-app-prod
    ports:
      - "8080:8080"
    environment:
      # Production profile and runtime overrides
      SPRING_PROFILES_ACTIVE: "prod"
      SPRING_DEVTOOLS_RESTART_ENABLED: "false"
      # Use external Postgres host for production. Set DB_HOST (and optionally DB_PORT) via environment or a .env file.
      DB_HOST: "${DB_HOST:-postgres.example.com}"
      DB_PORT: "${DB_PORT:-5432}"
    secrets:
      - DB_NAME
      - DB_USER
      - DB_PASSWORD
      - JWT_SECRET

# No local postgres service in this compose file for production; the application connects to a separate Postgres machine
secrets:
  DB_NAME:
    file: ../secrets/db_name
  DB_USER:
    file: ../secrets/db_user
  DB_PASSWORD:
    file: ../secrets/db_password
  JWT_SECRET:
    file: ../secrets/jwt_secret

